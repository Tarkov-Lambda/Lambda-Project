<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <AssemblyName>$(MSBuildProjectName)</AssemblyName>
    <LangVersion>9.0</LangVersion>
    <RootNamespace>$(MSBuildProjectName.Replace(" ", "_"))</RootNamespace>
    <DefineConstants>$(DefineConstants);EFT_RUNTIME</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Unity.Analyzers" Version="1.23.0" PrivateAssets="all" />
    <PackageReference Include="UnityEngine.Modules" Version="2022.3.43" IncludeAssets="compile" />
    <PackageReference Include="MemoryPack" Version="1.21.4" />
  </ItemGroup>

  <ItemDefinitionGroup>
    <Reference>
      <Private>false</Private>
    </Reference>
  </ItemDefinitionGroup>

  <ItemGroup>
    <!-- BepInEx / SPT -->
    <Reference Include="$(BepInExPath)\core\0Harmony.dll" />
    <Reference Include="$(BepInExPath)\core\BepInEx.dll" />
    <Reference Include="$(BepInExPath)\plugins\spt\spt-reflection.dll" />
    <Reference Include="$(BepInExPath)\plugins\Fika\Fika.Core.dll" />

    <Reference Include="$(ManagedPath)\*.dll" Exclude="$(ManagedPath)\mscorlib.dll;$(ManagedPath)\netstandard.dll" />

    <!-- <Reference Include="..\deps\libraries\*.dll" /> -->
    <!-- <Reference Include="..\deps\libraries\UniTask.dll" />
    <Reference Include="..\deps\libraries\UniTask*.dll" /> -->
  </ItemGroup>

  <Target Name="CopyFilesAfterBuild" AfterTargets="Build">
    <ItemGroup>
      <FilesToCopy Include="$(OutputPath)\$(MSBuildProjectName).dll" />
    </ItemGroup>

    <Copy SourceFiles="@(FilesToCopy)" DestinationFolder="$(SPTPath)\BepInEx\plugins\ifp"
      SkipUnchangedFiles="true" ContinueOnError="WarnAndContinue" />
  </Target>
</Project>